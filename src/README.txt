Make sure all source files are in the same directory.
To start the server, run the command ‘sudo python3 DNS.py’
Ensure that Python3 is installed on your system. 
Import necessary libraries, including `ip2geotools` and `dnslib`.
Web server IP addresses will be fetched from brahmarshi-arvind.online.txt file, which is in the same src directory.
Make sure all instances (web servers and DNS servers) are up and running.
Open a terminal in the directory containing the src code files. (cd command)


Python Libraries:
Pip3 install dnslib
Pip3 install ip2geotools

Apache benchmark:
Ubuntu:     apt-get install apache2-utils 
Fedora: 	 yum install httpd-tools